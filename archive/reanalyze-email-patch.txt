// ─────────────────────────────────────────────────────────────────────────────
// PATCH: app/api/cron/reanalyze-email/route.ts
//
// Two changes only:
//
// CHANGE 1 — Add import at the top of the file (after existing imports):
//   import { sendPushToUser } from '@/lib/push'
//
// CHANGE 2 — After the `sent++` line inside the email send loop, add the
//            push call below. Copy the block exactly as shown.
// ─────────────────────────────────────────────────────────────────────────────


// ── CHANGE 1: Add this import line after the existing imports ─────────────────
//
//   import { decrypt }           from '@/lib/encryption'
//   import { sendPushToUser }    from '@/lib/push'          ← ADD THIS LINE
//


// ── CHANGE 2: Find this block in the send loop (around line 175) ─────────────
//
//        sent++
//        console.log(
//          `[ReanalyzeEmail] Sent ${emailType} → ...`,
//        )
//
// Replace with:
//
//        sent++
//        console.log(
//          `[ReanalyzeEmail] Sent ${emailType} → ${email.slice(0, 3)}***@*** ` +
//          `(session ${session.id.slice(0, 8)})`,
//        )
//
//        // Also fire a push notification (non-blocking — email is the primary channel)
//        sendPushToUser(session.user_id as string, {
//          title: `${days} days later`,
//          body:  `"${bodySnippet}" — how's it sitting?`,
//          url:   `${appUrl}/record/${session.id}`,
//        }).catch(err => console.error('[ReanalyzeEmail] Push failed:', err))
//
// ─────────────────────────────────────────────────────────────────────────────
//
// That's it. No other changes to this file.
// ─────────────────────────────────────────────────────────────────────────────
